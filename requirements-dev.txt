# =====================================================================
# 📦 SpectraMind V50 — Development Dependencies (dev-only, hardened)
# ---------------------------------------------------------------------
# Scope:
#   Dev/test/lint/type/docs/security deps only (no runtime libs).
#
# Usage:
#   pip install -r requirements-dev.txt
#
# If tests need runtime libs, install both:
#   pip install -r requirements-kaggle.txt -r requirements-dev.txt
# =====================================================================

# --- Testing ----------------------------------------------------------
pytest==8.4.2
pytest-cov==6.0.0
pytest-xdist==3.6.1
pytest-timeout==2.3.1
pytest-rerunfailures==14.0
hypothesis==6.112.0
coverage==7.6.1          # CLI; complements pytest-cov

# --- Linting & Style --------------------------------------------------
ruff==0.6.3              # matches pre-commit hook rev (astral-sh/ruff-pre-commit v0.6.3)
pre-commit==3.7.1
toml-sort==0.23.1
codespell==2.3.0

# Markdown formatting (keeps parity with pre-commit)
mdformat==0.7.17
mdformat-gfm==0.3.6
mdformat-frontmatter==2.0.8
mdformat-footnote==0.1.2

# --- Type Checking ----------------------------------------------------
mypy==1.11.2             # matches pre-commit mirror
typing-extensions==4.12.2
types-PyYAML==6.0.12.20240808
types-requests==2.32.0.20240907
types-setuptools==75.1.0.20240917

# --- JSON / Schema Validation ----------------------------------------
check-jsonschema==0.33.3
jsonschema==4.23.0       # lockstep with runtime pin

# --- Notebook Hygiene -------------------------------------------------
nbstripout==0.7.1
nbqa==1.9.1              # aligns with (optional) nbqa hooks

# --- Docs / Diagrams --------------------------------------------------
mkdocs==1.6.0
mkdocs-material==9.5.39
pymdown-extensions==10.8.1
mkdocs-mermaid2-plugin==1.1.1

# --- Security / SBOM --------------------------------------------------
pip-audit==2.7.3
cyclonedx-bom==4.3.2
# bandit==1.7.9          # enable if you wire it in pre-commit

# --- Packaging / Release ----------------------------------------------
packaging==24.2
build==1.2.2.post1
twine==5.1.1
commitizen==3.27.0
cz-conventional-changelog==0.5.2

# --- Repo Utilities (used by Makefile/version script) -----------------
tomli-w==1.0.0           # needed by Makefile PY_SET_VERSION helper
