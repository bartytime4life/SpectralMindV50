# =====================================================================
# 📦 SpectraMind V50 — Development Dependencies
# ---------------------------------------------------------------------
# Purpose:
#   Extra dev/test/lint/type-checking dependencies beyond runtime.
#   Used locally and in CI for code quality, reproducibility, and safety.
#
# Notes:
#   • Versions pinned for stability (Kaggle/CI reproducibility).
#   • Keep in sync with pyproject.toml / setup.cfg / pre-commit.
#   • Runtime deps (e.g., torch, numpy, pandas, polars) live in runtime files.
# =====================================================================

# --- Testing ----------------------------------------------------------
pytest==8.4.2              # unit/integration tests
pytest-cov==6.3.0          # coverage reporting
pytest-xdist==3.8.0        # parallel test execution
pytest-timeout==2.4.0      # fail fast on hangs
hypothesis==6.112.0        # property-based tests (optional but powerful)

# --- Linting & Style --------------------------------------------------
ruff==0.12.12                # linter + formatter (supersedes flake8/isort/black)
toml-sort==0.23.1          # keep pyproject.toml sorted/stable
pre-commit==3.7.1          # run linters/formatters consistently

# --- Type Checking ----------------------------------------------------
mypy==1.17.1               # static typing
typing-extensions==4.12.2  # stabilize typing features for mypy/py3.11+
types-PyYAML==6.0.12.20240808
types-requests==2.32.0.20240907

# --- JSON / Schema Validation ----------------------------------------
check-jsonschema==0.33.3
# Keep dev in lockstep with runtime to avoid resolver drift:
jsonschema==4.22.0

# --- Data Science Utils (dev convenience) -----------------------------
# Mirror runtime pins to prevent test env skew.
pandas==2.2.2
polars==1.33.0

# --- CLI / Rich Output ------------------------------------------------
rich==13.7.1               # colored logs, improved CLI UX

# --- Docs / Diagrams --------------------------------------------------
mkdocs==1.6.0
mkdocs-material==9.5.39
pymdown-extensions==10.8.1
# Optional MkDocs plugins (uncomment if you use them)
# mkdocs-mermaid2-plugin==1.1.1
# mkdocs-gen-files==0.5.0
# mkdocs-literate-nav==0.6.1

# --- Security / SBOM --------------------------------------------------
pip-audit==2.7.3           # Python supply-chain vuln audit
cyclonedx-bom==4.3.12      # SBOM generation (CycloneDX)
# bandit==1.7.9             # (optional) static security analysis of Python code

# --- Packaging / Release (optional but useful in CI) ------------------
packaging==24.1
build==1.2.2.post1
twine==5.1.1
